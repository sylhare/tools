# Hoist dependencies to root node_modules for compatibility with tools that expect flat structure
shamefully-hoist=true

# Allow installation even when peer dependency versions don't match exactly
strict-peer-dependencies=false

